#!/bin/bash
#Generated by JetBrains Toolbox 3.5.0.84344 at 2026-06-11T08:30:55

declare -a intellij_args=()
declare -- wait=""

for o in "$@"; do
  if [[ "$o" = "--wait" || "$o" = "-w" ]]; then
    wait="-W"
    o="--wait"
  fi
  if [[ "$o" =~ " " ]]; then
    intellij_args+=("\"$o\"")
  else
    intellij_args+=("$o")
  fi
done

open -na "/Users/ian/Applications/IntelliJ IDEA.app/Contents/MacOS/idea" $wait --args "${intellij_args[@]}"